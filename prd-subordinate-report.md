# 下級報表 功能規格書

**代理後台 · PRODUCT REQUIREMENTS DOCUMENT**

代理後台｜下級報表｜v1.3｜2026-09-10

| 文件項目 | 內容 |
| --- | --- |
| 文件性質 | 功能規格書（PRD），描述代理後台「下級報表」頁之範圍、畫面、規則與驗收。 |
| 與現有文件關係 | 承接營運方開立總代帳號時設定之傭金方案；總代與其下級代理僅讀取生效模式、比例與方案結算週期，無權自行變更。 |
| 本版目的 | 定義代理查看直屬下級整條線業績與傭金之報表，含搜尋、匯總、列表、日期區間、分頁、公式提示與匯出。 |
| 適用範圍 | 代理後台。僅代理帳號可查看下級組織與報表；代理只能向下查看自己的下級組織，不提供向上查看上級組織。列表只列直屬下級，每列業績包含該下級本人與其所有下層團隊，樹深度不限。 |
| 不在本版範圍 | 總控／營運後台報表、玩家端代理中心、CPA 欄位與計算邏輯（後續版本新增）、首充與優惠欄位、頁內傭金模式切換、帳戶停用／恢復操作。 |
| 上游依據 | 營運方於總代帳號建立時設定之傭金方案（模式、比例、方案結算週期）；代理上下級樹；邀請碼；區間業績資料。 |

## 目錄

| 章節 | 內容 | 主要讀者 |
| --- | --- | --- |
| 01 | 這頁是什麼 | 全部 |
| 02 | 這頁不做什麼 | 產品／開發 |
| 03 | 名詞說明 | 全部 |
| 04 | 畫面內容 | 設計／開發 |
| 05 | 欄位說明 | 開發／QA |
| 06 | 狀態說明 | 開發／QA |
| 07 | 查詢、排序與分頁 | 開發／QA |
| 08 | 操作與跳轉 | 開發／設計 |
| 09 | 頁面狀態 | 開發／QA |
| 10 | User Story — 使用者 | 產品／開發 |
| 11 | 開發實作邊界 | 開發 |
| 12 | User Story — QA 驗證者 | QA |
| 13 | 驗收標準 | QA／產品 |
| 14 | 這頁需要的資料 | 開發／後端 |
| 15 | 本版確認狀態與後續版本 | 產品 |
| 16 | 版本沿革 | 全部 |
| 17 | 交付檢查表 | 專案／產品 |

# 1. 這頁是什麼

下級報表是代理後台的業績對帳頁。代理用來查看自己的**直屬下級**，以及每個直屬下級所代表的整條代理線，在指定日期區間內的有效投注、充值、淨輸贏、傭金差與應付傭金，並可分頁檢視及匯出。

列表只列當前代理的直屬下級，不將二級、三級或更深層代理攤平成獨立列。但每個直屬下級列的業績，需遞迴包含該直屬下級本人與其全部下層團隊，代理樹深度不設上限。

傭金方案由營運方在開立總代帳號時決定，包含計傭模式、比例與方案結算週期。總代與其下級代理僅沿用並查看生效方案，無權自行變更。本頁不提供模式切換。本版原型示例為**充值額方案**，因此頁面只展示充值額方案對應的傭金公式，不展示其他模式公式。

共用極差規則：

```text
傭金差（極差） = max(你的比例 − 直屬下級比例, 0)
```

極差只比較相鄰一層，也就是「你」和「你的直屬下級」，不可跨級直接比較更深層代理比例。

列上業績口徑：

```text
列上業績 = 該直屬下級整條線（本人 + 所有下層團隊）
```

本版原型充值額方案：

```text
計傭基數 = 該直屬下級整條線成功到帳充值（不含優惠／贈金）
傭金 = 計傭基數 × 傭金差
頁面傭金合計 = Σ 各直屬下級傭金
```

淨輸贏採亞洲代理報表口徑：

```text
平台盈虧 = 已結投注 − 派彩
淨輸贏 = 平台盈虧 − 返水 − 優惠
```

淨輸贏正數代表平台盈利，負數代表平台虧損；金流費不納入本頁代理淨輸贏。由於本原型使用充值額方案，淨輸贏僅作業績資訊展示，不作為本代理線計傭基數。

頁面由三塊組成：

1. 搜尋範圍匯總
2. 搜尋條件
3. 直屬下級列表（含固定表格視窗、分頁與匯出）

原型參考：`agent-subordinate-report-revised-v9.html`

# 2. 這頁不做什麼

| 不做的事 | 規則／由誰負責 |
| --- | --- |
| 在本頁設定或修改傭金方案、返佣比例、方案結算週期 | 傭金方案由營運方在開立總代帳號時決定；總代與其下級代理僅可查看與沿用，無權自行變更。 |
| 在本頁切換充值額／淨輸贏／有效投注等傭金模式 | 代理線沿用總代建立時已確定的方案模式，本頁只讀展示。 |
| 展示其他未生效傭金模式的計算公式 | 僅展示當前代理線實際生效方案的公式。 |
| 展示首充、優惠、活動贈金、返水成本明細 | 不對代理開放；淨輸贏只顯示計算後結果。 |
| 計算或展示 CPA | CPA 為獨立計酬項目，不等同其他傭金名稱；本版不展示，後續版本將新增獨立欄位與計算邏輯。 |
| 在本報表頁執行代理帳號停用／恢復 | 上級代理可停用或恢復自己的下級；代理可停用自己。代理自行停用後，僅能由營運方恢復。相關操作不在本報表頁。 |
| 向上查看上級代理或上級組織內容 | 此為代理後台的下級報表，只允許向下查看自己的下級組織，不提供向上查看。 |
| 查看非自己直屬下級作為獨立報表列 | 本頁列表只列自己的直屬下級；更深層代理只計入所屬直屬線業績。 |
| 顯示代理層級欄位或提供層級篩選 | 代理樹可無限向下，不以固定 N 層作為本頁查詢條件。 |
| 顯示官網、落地頁等註冊來源路徑 | 本頁僅展示邀請碼。 |
| 注單明細、充值明細、提現明細下鑽頁 | 對應明細頁；本版不做跳轉。 |
| 在頁面內處理停用帳戶狀態 | 被停用代理無法進入代理後台，因此不會進入本頁；若為自行停用，恢復需由營運方處理。 |

# 3. 名詞說明

| 名詞 | 說明 |
| --- | --- |
| 當前代理／你 | 已登入且狀態有效的代理帳號。 |
| 直屬下級 | 與當前代理只差一層、由其直接發展的下一層代理。 |
| 團隊／下層代理 | 某直屬下級再往下發展的所有代理，深度不限。 |
| 整條線 | 該直屬下級本人＋其全部下層團隊。本頁列上業績取整條線。 |
| 邀請碼 | 代理用於發展下級的邀請碼。本頁不展示官網／落地頁等來源路徑。 |
| 結算週期（查詢） | 本頁日期區間篩選，單次最多 90 個日曆日，不可選未發生日期。 |
| 方案結算週期 | 方案設定中的日結／週結／月結類型，本頁只讀展示，與查詢日期不是同一欄。 |
| 傭金模式 | 由營運方在開立總代帳號時設定，總代與其下級代理沿用同一生效模式，本頁不可自行切換。 |
| 你的比例 | 當前登入代理在生效方案下的返佣比例。 |
| 直屬下級比例 | 該列直屬下級代理的返佣比例。 |
| 傭金差／極差 | `max(你的比例 − 直屬下級比例, 0)`；只比較相鄰一層，不跨級。 |
| 有效投注 | 查詢區間內、已歸戶之有效投注合計；每列取該直屬下級整條線。 |
| 充值 | 查詢區間內成功到帳充值，不含優惠／贈金；每列取該直屬下級整條線。 |
| 平台盈虧 | `已結投注 − 派彩`。正數為平台盈利，負數為平台虧損。 |
| 淨輸贏 | `平台盈虧 − 返水 − 優惠`；金流費不納入本頁口徑。 |
| 傭金 | 本版充值額方案為「該直屬下級整條線成功到帳充值 × 傭金差」。 |
| CPA | 獨立的計酬項目，本版尚未展示；後續版本將新增專屬欄位與計算邏輯，不與目前極差傭金混用。 |
| 活躍玩家 | 區間內有有效投注的玩家 UID。列內按該整條線去重；匯總卡按本次查詢結果跨列再去重。 |
| 查詢合計 | 本次搜尋條件下全部結果的合計，不受當前分頁限制。 |
| 本頁數據 | 目前頁碼在當前每頁筆數設定下所呈現的資料。 |
| 全部數據 | 目前搜尋條件下的全部直屬下級結果，跨越所有頁碼。 |

# 4. 畫面內容

| 區塊 | 內容 |
| --- | --- |
| Header | Breadcrumb：代理後台／報表中心／下級報表。Page Title：下級報表。右側以唯讀 Chip 顯示方案模式與方案結算週期。 |
| KPI Summary | 四張 KPI Card，依本次搜尋全部結果計算：有效投注、充值、傭金、活躍玩家。傭金 Card 旁提供公式 Tooltip。 |
| Search Form | 由左至右、可換行：代理 UID（Input）→ 邀請碼（Input）→ 結算週期（Date Range Picker）→ 搜尋／重置 Button。右上顯示目前查詢區間。 |
| Date Range Picker | Trigger 顯示「起始日 ～ 結束日」。Click 後開啟 Popover，內含本週／本月／上月／近 90 日快捷、雙月曆、清除與確定；按確定後才套用。 |
| Date Rule Hint | Date Range Picker Popover 內顯示「單次查詢最多 90 日；不可選取未發生日期；近 90 日以今日為最後一天」。 |
| Subordinate Table | Table 只列直屬下級。欄位超過寬度可 Horizontal Scroll；資料超過視窗高度可 Vertical Scroll。 |
| Table Viewport | Table 使用固定高度 viewport，避免大量資料撐長整頁；Table Header 使用 Sticky Header，查詢合計列使用 Sticky Footer。 |
| Pagination | Table 下方提供 Page Size 10／15／30／50／100，預設 15；另有 Page Number、Previous／Next 與「第 X–Y 筆，共 N 筆」資訊。 |
| Export Dropdown | 「匯出報表」Click 後開啟 Dropdown Menu，提供「下載本頁數據／下載全部數據」。 |
| Formula Tooltip | 有明確計算公式之欄位旁顯示 `!`。Hover 或鍵盤 Focus 時顯示 Tooltip，不改變 Table 高度或欄寬。 |
| Toast | 匯出完成後於頁面上方顯示匯出範圍、筆數與檔名，約 4 秒後自動關閉。 |

搜尋列排序規則：

1. Input（代理 UID、邀請碼）
2. Date Range Picker（結算週期）
3. Action Button（搜尋、重置）

寬度不足時以「整組」換行，不可拆散 Date Range Picker。

# 5. 欄位說明

| # | 欄位 | 內容 | 無資料時顯示 |
| --- | --- | --- | --- |
| 1 | 代理 UID | 直屬下級代理帳號／UID，本列主鍵。 | — |
| 2 | 邀請碼 | 該直屬下級代理邀請碼。 | — |
| 3 | 有效投注 | 該直屬下級整條線、查詢區間內有效投注，兩位小數。 | 0.00 |
| 4 | 充值 | 該直屬下級整條線成功到帳充值，不含優惠／贈金，兩位小數。 | 0.00 |
| 5 | 淨輸贏 `!` | 該直屬下級整條線淨輸贏，兩位小數，可為負。Hover 顯示亞洲口徑公式 Tooltip。 | 0.00 |
| 6 | 你的比例 | 當前代理方案比例，如 1.20%。全表相同。 | 0.00% |
| 7 | 下級比例 | 該列直屬下級方案比例。 | 0.00% |
| 8 | 傭金差 `!` | `max(你的比例 − 直屬下級比例, 0)`，只比較相鄰一層。 | 0.00% |
| 9 | 傭金 `!` | 本原型充值額方案：整條線成功到帳充值 × 傭金差。單列傭金以小數 2 位作為報表計算與展示值。 | 0.00 |
| 10 | 活躍玩家 | 該列整條線區間內有有效投注之玩家 UID 去重人數。 | 0 |

補充：

- 來源資訊只展示「邀請碼」。
- 表格只列直屬下級；二級以下代理資料只進入所屬直屬下級的整條線業績，不另外成列。
- 匯總卡「有效投注／充值／傭金」為各直屬下級列的本次查詢總計。
- 活躍玩家不可把各列人數直接相加，匯總需跨列以玩家 UID 聯集去重。
- 金額／傭金在報表頁展示兩位小數；傭金合計以各列同一個小數 2 位報表值加總，避免畫面逐列相加與合計出現 0.01 差異。不另做整數進位；代理提款僅能提取整數屬提款流程規則。

# 6. 狀態說明

| 狀態 | 顯示／規則 |
| --- | --- |
| 生效方案模式 | 頁首晶片只讀顯示資料庫回傳之模式。本原型為「方案模式：充值額」。 |
| 充值額方案公式 | 本原型只展示 `傭金＝直屬下級整條線成功到帳充值 × max(你的比例−直屬下級比例,0)`。 |
| 其他傭金模式 | 本原型不展示其他模式公式；正式環境如代理線為其他模式，由方案資料決定頁面對應展示內容。 |
| 方案結算週期 | 頁首晶片「方案結算：×結」，為方案結算節奏，不是搜尋日期。 |
| 快捷＝本週 | 週一至今日。今日以前才可選，未發生日期不可帶入。 |
| 快捷＝本月 | 當月 1 日至今日。 |
| 快捷＝上月 | 上月 1 日至上月最後一日。 |
| 快捷＝近 90 日 | 今日為最後一天，向前含今日共 90 個日曆日。 |
| 自選區間 | 快捷不高亮；起始與結束皆不得晚於今日，含起訖最多 90 日。 |
| Loading | 表格顯示載入中狀態；搜尋及相關操作暫時鎖定，避免重複請求。 |
| 查無資料 | 表格顯示「查無相關數據」與調整搜尋條件提示。 |
| 匯出選單 | 點擊匯出報表後才顯示「下載本頁數據／下載全部數據」。 |

日期計算基準：

- 後台結算時區以 `Asia/Taipei`（UTC+8）為本版基準。
- 本週週起始固定為週一。
- 本週、本月及近 90 日的結束日均以「今日」為最晚日期。
- 日曆上今日之後的日期直接 disabled，不可選取。
- 選定第一個日期後，與其相距超過 90 日的第二日期不可選取。

# 7. 查詢、排序與分頁

| 欄位／功能 | 型態 | 說明 |
| --- | --- | --- |
| 代理 UID | 精確匹配 | 完整 UID 才命中。空白＝不限。僅搜尋當前代理的直屬下級。 |
| 邀請碼 | 模糊匹配 | 包含即可，不分大小寫。空白＝不限。僅搜尋直屬下級邀請碼。 |
| 結算週期 | 日期區間 | 起始日不可晚於結束日；不可選未發生日期；含起訖最多 90 日；可跨月。 |
| 預設條件 | 進頁即查 | 本週（週一～今日）＋ UID／邀請碼空白。 |
| 重置 | 按鈕 | 清空 UID、邀請碼；日期恢復本週；回第 1 頁並立即重查。 |
| 排序 | 本版 | 固定依直屬下級 UID 升序。本版不做點表頭排序。 |
| 分頁 | 本版 | 每頁可選 10／15／30／50／100 筆，預設 15 筆。 |
| 換頁 | 本版 | 上一頁／下一頁／頁碼；換頁只改當前列表資料，不改本次查詢合計。 |
| 每頁筆數切換 | 本版 | 切換後回到第 1 頁。 |
| 查詢合計 | 本版 | KPI 與表尾查詢合計一律以本次搜尋全部結果計算，不受分頁影響。 |
| 零業績列 | 本版 | 直屬下級只要符合 UID／邀請碼／日期等查詢條件，即使區間內有效投注、充值、淨輸贏皆為 0，仍保留該列並按欄位規則顯示 0.00／0，不因零業績移除。 |
| 表格視窗 | 本版 | 固定高度；表格內上下／左右滾動，不讓大量列數持續撐長整頁。 |

所有搜尋條件為 AND。

正式環境可由後端執行分頁；不論前端或後端分頁，匯總、總筆數與「下載全部數據」均代表完整搜尋結果，不得只取當前頁。

# 8. 操作與跳轉

| 點擊位置 | 結果 | 帶入／規則 |
| --- | --- | --- |
| 搜尋 | 停留本頁，進入 Loading 後重算匯總與列表 | 目前 UID、邀請碼、日期區間；查詢完成回第 1 頁 |
| 重置 | 停留本頁，恢復預設條件並重查 | UID／邀請碼清空；日期＝本週 |
| 結算週期觸發器 | 開啟 Date Range Picker | 帶入目前已套用起迄 |
| 本週 | 寫入暫存起迄，不立刻關閉 | 當週週一～今日 |
| 本月 | 寫入暫存起迄，不立刻關閉 | 當月 1 日～今日 |
| 上月 | 寫入暫存起迄，不立刻關閉 | 上月完整月份 |
| 近 90 日 | 寫入暫存起迄，不立刻關閉 | 今日往前含今日共 90 日 |
| 日曆日期 | 第一次＝起始，第二次＝結束；若第二次較早則對調 | 未發生日期與超過 90 日可選範圍之日期 disabled |
| 清除 | 清空暫存起迄 | 無 |
| 確定 | 驗證日期規則，合法則關閉元件、套用區間並重查 | 暫存起迄 |
| 每頁筆數 | 重切當頁資料 | 10／15／30／50／100；切換後回第 1 頁 |
| 上一頁／下一頁／頁碼 | 換頁 | 不重算查詢條件，不改查詢合計 |
| 匯出報表 | 展開匯出範圍選單 | 不直接下載 |
| 下載本頁數據 | 下載 XLSX | 只匯出目前頁面資料與該範圍合計；檔名：`下級報表_YYYYMMDD~YYYYMMDD_本頁_第N頁.xlsx` |
| 下載全部數據 | 下載 XLSX | 匯出目前搜尋條件全部結果；檔名：`下級報表_YYYYMMDD~YYYYMMDD_全部.xlsx` |
| 公式 `!` | 顯示公式 Tooltip | 滑鼠 Hover 或鍵盤 Focus；離開／Blur 關閉 |
| 代理 UID（本版） | 不跳轉 | 本版不做下鑽 |

# 9. 頁面狀態

| 狀態 | 顯示方式 |
| --- | --- |
| 載入中 | 表格中央顯示 spinner、「資料載入中…」及「正在依目前條件取得報表資料，請稍候。」 |
| Loading 鎖定 | 搜尋按鈕顯示「搜尋中…」且不可重複點擊；重置、匯出、每頁筆數、分頁操作暫時不可用。 |
| 查詢成功有資料 | 匯總有數；表格顯示當前頁資料；查詢合計為全部搜尋結果；筆數顯示全部結果筆數。 |
| 查詢成功無資料 | 沒有任何符合 UID／邀請碼／日期條件的直屬下級時，匯總歸 0；表格顯示「查無相關數據」，並提示調整搜尋條件；分頁無可操作頁面。 |
| 查詢成功但零業績 | 直屬下級符合查詢條件但區間業績皆為 0 時，仍顯示該直屬下級資料列，各數值欄依規則顯示 0.00／0，不視為「查無相關數據」。 |
| 起始日晚於結束日 | 不套用查詢；提示「起始日不可晚於結束日」。 |
| 超過 90 日 | 不套用查詢；提示「單次查詢最多可選 90 日」；日曆第二日期超範圍時直接不可選。 |
| 選到未發生日期 | 未來日期在日曆中 disabled；如資料異常帶入則驗證失敗並提示「不可選取未發生日期」。 |
| 未選完整起迄即確定 | 確定無效，元件保持開啟。 |
| 匯出選單開啟 | 顯示「下載本頁數據」與「下載全部數據」，並標示各自預計匯出筆數。 |
| 無可匯出資料 | 不下載，Toast 顯示「目前沒有可匯出的數據」。 |
| 匯出本頁成功 | 開始下載；Toast：`已下載本頁數據：第 N 頁，共 X 筆｜{檔名}`，約 4 秒。 |
| 匯出全部成功 | 開始下載；Toast：`已下載全部數據：共 X 筆｜{檔名}`，約 4 秒。 |
| 帳戶停用 | 不屬本頁狀態；被停用代理無法進入代理後台。上級代理可停用／恢復自己的下級；代理自行停用後僅能由營運方恢復。 |

# 10. User Story — 使用者

| **US-U01 查看直屬下級整條線業績與傭金** |
| :--- |
| 身為代理，我要看到每個直屬下級所代表整條線的投注、充值、淨輸贏與傭金，以便對帳。 |
| **GIVEN** |
| 我已登入代理後台，帳戶有效，且方案已有傭金模式與比例。 |
| **WHEN** |
| 我進入下級報表或調整條件後按搜尋。 |
| **THEN** |
| 列表只顯示我的直屬下級；每列業績包含該直屬下級本人及全部下層團隊；傭金只按我與該直屬下級的極差計算。 |

| **US-U02 用受限制日期區間查資料** |
| :--- |
| 身為代理，我要查本週、本月、上月、近 90 日或自選區間，且不能選未來資料。 |
| **GIVEN** |
| 我在下級報表頁。 |
| **WHEN** |
| 我打開結算週期元件，選快捷或點起始～結束後按確定。 |
| **THEN** |
| 本週固定週一到今日；本月到今日；近 90 日以今日為最後一天；自選最多 90 日且不可選未發生日期。 |

| **US-U03 分頁查看大量直屬下級** |
| :--- |
| 身為代理，我可能有超過 100 名直屬下級，我要在固定表格視窗內分頁查看資料。 |
| **GIVEN** |
| 查詢結果超過一頁。 |
| **WHEN** |
| 我切換每頁筆數或頁碼。 |
| **THEN** |
| 可選 10／15／30／50／100 筆，預設 15；表格可上下左右滾動，查詢合計不因換頁改變。 |

| **US-U04 選擇匯出本頁或全部資料** |
| :--- |
| 身為代理，我要依用途決定只下載現在看到的頁面，或下載整個搜尋結果。 |
| **GIVEN** |
| 我已查出至少 1 筆結果。 |
| **WHEN** |
| 我點擊匯出報表。 |
| **THEN** |
| 先看到「下載本頁數據／下載全部數據」兩個選項；選擇後才下載 XLSX，Toast 明確告知匯出範圍與筆數。 |

| **US-U05 查看計算公式** |
| :--- |
| 身為代理，我需要理解淨輸贏、極差與傭金數字怎麼來。 |
| **GIVEN** |
| 欄位存在明確計算公式。 |
| **WHEN** |
| 我 Hover `!` 或用鍵盤 Focus `!`。 |
| **THEN** |
| 顯示該欄位公式；本代理線只展示目前充值額方案的傭金公式，不展示未生效模式公式。 |

# 11. 開發實作邊界

本 PRD 僅定義產品行為、資料口徑與驗收結果，不在 User Story 中描述資料欄位命名、回傳結構或程式實作方式。

開發實作需確保：

- 生效傭金方案為唯讀，總代與下級代理不可在本頁修改。
- 列表只呈現當前代理的直屬下級，每列業績包含該直屬下級全部下層團隊。
- 極差只比較相鄰一層，不跨級比較。
- 分頁不改變 KPI、查詢合計與完整結果筆數的產品口徑。
- 被停用代理在進入本頁前即無法進入代理後台；帳號停用／恢復規則依帳號管理流程執行。

具體產品驗收以第 13 章「驗收標準」及第 14 章「這頁需要的資料」為準。

# 12. User Story — QA 驗證者

| **US-Q01 驗證直屬列與無限團隊彙總** |
| :--- |
| 身為 QA，我要確認表格只列直屬下級，但深層業績都有進入正確直屬線。 |
| **GIVEN** |
| 測試帳號有多個直屬下級，其中至少一條線深度超過 5 層。 |
| **WHEN** |
| 我查詢全部結果。 |
| **THEN** |
| 深層代理不獨立成列；其業績只彙入所屬直屬下級列。 |

| **US-Q02 驗證搜尋欄位** |
| :--- |
| 身為 QA，我要確認只剩 UID、邀請碼、日期三類查詢條件。 |
| **GIVEN** |
| 我打開本頁。 |
| **WHEN** |
| 我檢查搜尋列與表頭。 |
| **THEN** |
| 沒有層級／路徑篩選，也沒有層級／路徑欄位；邀請碼可模糊搜尋。 |

| **US-Q03 驗證日期限制** |
| :--- |
| 身為 QA，我要確認不能選未發生日期且單次最多 90 日。 |
| **GIVEN** |
| 今日為任一日期。 |
| **WHEN** |
| 我開啟日期元件並操作快捷、自選區間。 |
| **THEN** |
| 本週為週一～今日；今日之後 disabled；自選含起訖不可超過 90 日；近 90 日以今日為最後一天。 |

| **US-Q04 驗證公式提示** |
| :--- |
| 身為 QA，我要確認 `!` 不影響表格排版且公式內容正確。 |
| **GIVEN** |
| 本代理線為充值額方案。 |
| **WHEN** |
| Hover／Focus 淨輸贏、傭金差、傭金與傭金匯總提示。 |
| **THEN** |
| Tooltip 正確顯示亞洲淨輸贏、相鄰極差與充值額傭金公式，不展示其他傭金模式公式。 |

| **US-Q05 驗證 Loading 與空資料** |
| :--- |
| 身為 QA，我要確認查詢期間不可重複操作，查不到資料時有明確空狀態。 |
| **GIVEN** |
| 我送出搜尋或使用不會命中的搜尋條件。 |
| **WHEN** |
| 查詢進行中或完成為 0 筆。 |
| **THEN** |
| Loading 顯示 spinner／文字並鎖定搜尋等操作；0 筆顯示「查無相關數據」。 |

| **US-Q06 驗證分頁** |
| :--- |
| 身為 QA，我要確認大量資料可用多種每頁筆數查看。 |
| **GIVEN** |
| 結果超過 100 筆。 |
| **WHEN** |
| 我切換 10／15／30／50／100 或換頁。 |
| **THEN** |
| 預設 15；頁碼與第 X–Y 筆正確；KPI／查詢合計不因頁碼改變。 |

| **US-Q07 驗證匯出範圍與 Toast** |
| :--- |
| 身為 QA，我要確認匯出前可選本頁或全部。 |
| **GIVEN** |
| 搜尋結果超過一頁。 |
| **WHEN** |
| 我分別下載本頁數據與全部數據。 |
| **THEN** |
| XLSX 筆數、合計、檔名與選定範圍一致；Toast 分別顯示本頁或全部數據的提示。 |

| **US-Q08 驗證固定表格視窗** |
| :--- |
| 身為 QA，我要確認選 100 筆時不會把整個頁面拉得過長。 |
| **GIVEN** |
| 當頁資料超過表格視窗高度或欄位總寬大於視窗。 |
| **WHEN** |
| 我檢視與滾動表格。 |
| **THEN** |
| 表格內可上下／左右滾動；表頭固定上方；查詢合計固定下方；分頁留在表格視窗外。 |

# 13. 驗收標準

| ID | 主題 | 驗收敘述 |
| --- | --- | --- |
| AC-01 | 進頁預設 | Given 代理進入本頁；When 未改條件；Then 日期為本週週一至今日，UID／邀請碼空白，自動查出結果，預設每頁 15 筆。 |
| AC-02 | 直屬下級 | Given 當前代理 A；When 查詢；Then 表格只列 A 的直屬下級，不將更深層代理獨立成列。 |
| AC-03 | 整條線取數 | Given 直屬下級 B 下有任意深度 C、D…；When 顯示 B 列；Then 有效投注、充值、淨輸贏與活躍玩家均包含 B 本人及全部下層團隊。 |
| AC-04 | UID 精確與零業績 | Given 輸入完整直屬下級 UID；When 搜尋；Then 僅命中對應直屬下級。若該直屬下級存在但區間有效投注、充值、淨輸贏皆為 0，仍須顯示 1 列並以 0.00／0 呈現；只有不存在或非直屬 UID 才為 0 筆。 |
| AC-05 | 邀請碼模糊 | Given 輸入邀請碼關鍵字；When 搜尋；Then 邀請碼包含該字（不分大小寫）的直屬下級均列出。 |
| AC-06 | 移除舊欄位 | Given 查看搜尋列、表頭與匯出；When 對照規格；Then 不存在層級與路徑欄位／篩選，只保留邀請碼。 |
| AC-07 | 日期最多 90 日 | Given 自選日期；When 起訖含首尾超過 90 日；Then 超範圍日期不可作為第二日期，且驗證不得套用超過 90 日區間。 |
| AC-08 | 未來日期 | Given 今日為 D；When 打開日曆；Then D+1 之後日期 disabled 且不可查詢。 |
| AC-09 | 本週 | Given 今日為週內任一天；When 點本週；Then 起始固定週一，結束固定今日，不帶到尚未發生的週日。 |
| AC-10 | 本月／上月／近90日 | Given 點各快捷；Then 本月＝1 日～今日；上月＝完整上月；近90日＝今日往前含今日共90日。 |
| AC-11 | 淨輸贏公式 | Given 任一列；When 檢查淨輸贏；Then `淨輸贏=(已結投注−派彩)−返水−優惠`，金流費不納入；正數＝平台盈利。 |
| AC-12 | 極差公式 | Given A 的直屬下級為 B；When 算 B 列；Then 傭金差＝`max(A比例−B比例,0)`，不得跨級直接比較 C、D 比例。 |
| AC-13 | 充值額傭金 | Given 本代理線方案＝充值額；When 算一列；Then 傭金＝該直屬下級整條線成功到帳充值 × 傭金差；充值不含優惠／贈金；單列傭金以小數 2 位作為報表值。 |
| AC-14 | 公式提示 | Given 查看有公式欄位；When Hover／Focus `!`；Then Tooltip 顯示對應公式且不改變表格高度／欄寬；本代理線不展示其他模式傭金公式。 |
| AC-15 | 活躍去重 | Given 不同直屬線結果集合存在相同玩家 UID；When 看匯總活躍玩家；Then 同一玩家只計 1，不直接相加列上人數。 |
| AC-16 | Loading | Given 點搜尋；When 查詢尚未完成；Then 表格顯示載入中，搜尋不可重複點擊，重置／匯出／每頁筆數／分頁暫時鎖定。 |
| AC-17 | 查無資料 | Given 條件無命中；When 查詢完成；Then 表格顯示「查無相關數據」與提示文字，匯總歸 0，分頁無可操作頁。 |
| AC-18 | 分頁選項 | Given 有足夠結果；When 查看每頁筆數；Then 選項為 10／15／30／50／100，預設 15；切換後回第 1 頁。 |
| AC-19 | 查詢合計 | Given 結果跨多頁；When 換頁；Then KPI 與表尾查詢合計代表全部搜尋結果，不因當前頁改變；傭金合計以各列小數 2 位報表傭金加總，與使用者逐列相加結果一致。 |
| AC-20 | 固定表格視窗 | Given 當頁資料很多或欄位超寬；When 查看表格；Then 可在表格區上下／左右滾動，表頭 sticky、查詢合計列 sticky，整頁不因 100 筆持續拉長。 |
| AC-21 | 匯出選擇 | Given 至少 1 筆資料；When 點匯出報表；Then 先顯示「下載本頁數據／下載全部數據」，不直接下載。 |
| AC-22 | 匯出本頁 | Given 選下載本頁；When 下載；Then XLSX 只包含當前頁資料，檔名含 `_本頁_第N頁`，Toast 顯示本頁數據、頁碼與筆數。 |
| AC-23 | 匯出全部 | Given 選下載全部；When 下載；Then XLSX 包含目前搜尋條件全部結果，檔名含 `_全部`，Toast 顯示全部數據筆數。 |
| AC-24 | 無資料匯出 | Given 查詢結果 0 筆；When 點匯出；Then 不產生檔案，Toast 顯示「目前沒有可匯出的數據」。 |
| AC-25 | 方案只讀 | Given 營運方已在開立總代帳號時確定傭金方案；When 總代或其下級代理使用本頁；Then 僅可查看生效模式、比例與方案結算週期，不提供修改或模式切換。 |
| AC-26 | 帳戶停用／恢復 | Given 上級代理停用下級，或代理自行停用；When 被停用代理嘗試進入代理後台；Then 無法進入本頁。上級代理可恢復其下級；代理自行停用後僅能由營運方恢復。 |
| AC-27 | 組織查看方向 | Given 代理已登入；When 查看下級報表；Then 只能向下查看自己的下級組織，不提供向上查看上級代理或上級組織內容。 |
| AC-28 | CPA | Given 使用本版下級報表；When 查看欄位與計算；Then 本版不展示 CPA 欄位或 CPA 計算；後續版本以獨立欄位與邏輯新增。 |

# 14. 這頁需要的資料

| 需求 | 內容 |
| --- | --- |
| 當前代理 | 代理 UID 與目前生效的傭金方案。只有可正常進入代理後台的有效代理會進入此頁。 |
| 生效傭金方案 | 當前代理線的計傭模式、方案結算週期、你的比例。方案由營運方在開立總代帳號時確定，總代與下級代理僅讀取。 |
| 直屬下級 | 直屬下級代理 UID、邀請碼與下級比例。 |
| 下級組織關係 | 完整的代理上下級關係，深度不限，用於將所有深層代理業績歸入正確的直屬下級整條線。 |
| 區間業績 | 可彙總至各直屬線的有效投注、成功充值、已結投注、派彩、返水、優惠與活躍玩家 UID。 |
| 淨輸贏 | 依本頁口徑取得或計算：`(已結投注−派彩)−返水−優惠`；金流費不納入。 |
| 查詢結果統計 | 本次搜尋全部結果的有效投注、充值、傭金、活躍玩家去重數與完整結果筆數。 |
| 分頁資訊 | 目前頁碼、每頁筆數與完整結果筆數；每頁可選 10／15／30／50／100，預設 15。 |
| 匯出資料 | 可分別取得目前頁面資料或目前搜尋條件下的全部資料；匯出欄位與畫面一致，輸出 XLSX。 |
| 日期基準 | 日期與日切基準使用 UTC+8；單次查詢最多 90 日且不可超過今日。 |

本章僅描述產品需要取得的資料內容，不定義開發端的欄位名稱、請求格式、回傳結構或程式碼。

# 15. 本版確認狀態與後續版本

## 15.1 本版待確認事項

本版無待確認事項。下級報表目前的產品行為、計算口徑、日期限制、分頁、匯出與驗收規則已可作為本版開發與 QA 依據。下列內容屬後續版本規劃或外部資料依賴，不阻擋本版。

## 15.2 後續版本規劃（不影響本版）

| # | 項目 | 說明 |
| --- | --- | --- |
| F-01 | 淨輸贏模式負值處理 | 未來若代理線採淨輸贏模式，再另行定義淨輸贏為負時的計傭規則；目前充值額方案不受影響。 |
| F-02 | 代理 UID 下鑽 | 本版代理 UID 不跳轉；後續若增加直屬下級報表／管理頁下鑽，再另行定義導航與權限。 |
| F-03 | CPA 欄位與計算 | CPA 為後續獨立計酬功能，屆時另行定義合格條件、計算方式與展示位置。 |

## 15.3 資料依賴

| # | 項目 | 來源／責任 |
| --- | --- | --- |
| E-01 | 營運方於總代帳號建立時確定之傭金模式、比例與方案結算週期。 | 營運／傭金方案資料 |
| E-02 | 直屬關係、無限層下級組織與邀請碼。 | 代理帳號／組織資料 |
| E-03 | 報表整條線聚合、完整查詢合計、完整結果筆數、分頁與本頁／全部匯出所需資料。 | 報表資料 |
| E-04 | 有效投注、已結投注、派彩、返水、優惠與活躍玩家 UID 的可靠來源及既有口徑。 | 遊戲／帳務資料 |

已確認事項：

- 傭金方案由營運方在開立總代帳號時決定；總代與其下級代理無權自行變更。
- 此為代理後台；代理只能向下查看自己的下級組織，不提供向上查看上級組織。
- CPA 為獨立計酬項目，本版不展示，後續將新增獨立欄位與計算邏輯。
- 上級代理可停用／恢復自己的下級；代理可自行停用。代理自行停用後僅能由營運方恢復。
- 本頁不再限制一～五級，也不提供層級欄位／篩選。
- 列表只列直屬下級，整條線遞迴深度不限。
- 符合查詢條件的直屬下級即使區間業績全部為 0，仍保留資料列並顯示 0.00／0。
- 極差只比較你與直屬下級。
- 註冊來源只展示邀請碼，不展示其他來源路徑。
- 日期單次最多 90 日，不可選未發生日期。
- 本週固定週一至今日；近 90 日以今日為最後一天。
- 分頁為 10／15／30／50／100，預設 15。
- 查詢合計與 KPI 為全部搜尋結果，不是當頁合計。
- 傭金單列與合計使用同一小數 2 位報表精度，合計以各列報表傭金相加。
- 匯出須先選「本頁數據」或「全部數據」。
- 金額／傭金在報表展示兩位小數；提款僅能提取整數的規則屬提款流程，不在本頁處理。

# 16. 版本沿革

| 版本 | 日期 | 內容 |
| --- | --- | --- |
| v1.0 | 2026-09-09 | 初版：下級報表含四卡匯總、UID／路徑／層級／日期搜尋、十一欄列表與單一匯出。 |
| v1.1 | 2026-09-10 | 依需求確認更新：路徑改邀請碼；移除層級欄位與篩選；改為只列直屬下級且整條線無限層彙總；極差只算相鄰層；原型使用充值額方案並僅展示當前方案公式；淨輸贏採亞洲口徑；日期最多90日且不可選未來；本週週一至今日；新增近90日；新增 Loading／查無資料；分頁10／15／30／50／100；匯出可選本頁／全部；固定表格視窗支援上下左右滾動與 Sticky Header／Sticky Footer。 |
| v1.2 | 2026-09-10 | 第二輪規格清理：傭金方案改為營運開立總代時決定、總代不可修改；明確代理僅可向下查看下級組織；CPA 改為獨立計酬項目並預留後續欄位；補充上級停用／恢復與自行停用後營運恢復規則；移除尚未提供功能與不存在制度的描述；UI 用語改為 Breadcrumb／Popover／Tooltip／Pagination 等開發常用術語；移除 User Story 與資料章節中的開發欄位／介面定義。 |
| v1.3 | 2026-09-10 | 原型／PRD 評審修正：零業績直屬下級仍保留顯示；淨輸贏統一使用「已結投注」用詞；移除未定義且正常流程不可達的匯出日期缺失分支；補足跨列共享玩家測試資料；傭金列值與合計統一採小數 2 位報表精度；移除淨輸贏儲存格原生 title 重複提示；第 15 章改為本版無待確認事項，將其他議題轉列後續版本。 |

# 17. 交付檢查表

| 檢查面 | 完成條件 | 確認 |
| --- | --- | --- |
| 產品 | 本規格 1～15 章已對過最新版原型與需求確認結果 |  |
| 設計 | 搜尋順序、日期規則、公式 `!` Tooltip、固定表格視窗、分頁、匯出選單與 Toast 已對稿 |  |
| 前端 | UID／邀請碼／日期搜尋、90 日與未來日期限制、Loading 鎖定、空狀態、固定表格捲動、分頁、匯出範圍、XLSX 檔名與 Toast |  |
| 後端 | 生效方案唯讀、只呈現直屬列、無限層整條線聚合、相鄰極差、完整查詢合計與筆數、分頁、本頁／全部匯出 |  |
| QA | AC-01～AC-28 可測且有超過 100 筆直屬下級、深度超過 5 層、跨列共享玩家、零業績直屬下級等測試資料 |  |
| 權限 | 有效代理僅可向下查看自己的下級組織；停用代理無法登入後台；代理自行停用後僅能由營運方恢復 |  |
| 日期 | 週一起算、不可選未來、最多 90 日、近 90 日以今日為結束日 |  |
| 傭金 | 原型充值額方案公式、相鄰極差、整條線計傭與小數 2 位列值／合計口徑一致 |  |
| 匯出 | 可選本頁／全部；XLSX 欄位、筆數、合計、檔名與 Toast 均符合選擇範圍 |  |
| 表格 | 固定高度；支援上下左右滾動；表頭與查詢合計 sticky；分頁位於視窗外 |  |
